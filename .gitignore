# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor junk
.vscode/
.idea/
*.swp

# Local development versions of the HTML — old ones with old API keys
Past Versions/

# Local API key versions — NEVER commit these
.env
.env.local
*-with-api-key.html
*-with-key.html
